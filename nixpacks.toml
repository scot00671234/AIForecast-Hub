# Minimal configuration to prevent all default file creation
providers = ["node"]

[variables]
NODE_ENV = "production"
PORT = "3000"

[phases.install]
cmds = ["npm ci --production=false"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"