# Clean Production Setup - Fixed build artifacts issue
providers = ["node"]

[variables]
NODE_ENV = "production"
PORT = "5000"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "rm -rf dist server/public public",
  "vite build",
  "mkdir -p server/public",
  "cp -r dist/public/* server/public/",
  "esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist --external:__dirname"
]

[start]
cmd = "cd /app && node dist/index.js"