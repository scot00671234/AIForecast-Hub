# ULTRA SIMPLE - Direct Node.js deployment
providers = ["node"]

[variables]
PORT = "5000"

[phases.install]
cmds = ["npm ci"]

[phases.build]  
cmds = ["chmod +x aggressive-cache-bust.sh", "./aggressive-cache-bust.sh"]

[start]
cmd = "node dist/index.js"