# ULTRA SIMPLE - Direct Node.js deployment
providers = ["node"]

[variables]
PORT = "5000"

[phases.install]
cmds = ["npm ci"]

[phases.build]  
cmds = ["chmod +x debug-production.sh", "./debug-production.sh"]

[start]
cmd = "node dist/index.js"