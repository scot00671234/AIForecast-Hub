providers = ["node"]

[variables]
NODE_ENV = "production"  
PORT = "3000"

[providers.node]
skipCaddy = true

[phases.build]
cmds = ["npm run build", "node fix-build.js"]

[start]
cmd = "node production-start.js"