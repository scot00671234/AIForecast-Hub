# CLEAN MINIMAL NODE.JS ONLY CONFIGURATION
providers = ["node"]

[variables]
NODE_ENV = "production"
PORT = "3000"

[phases.install] 
cmds = ["npm ci --production=false"]

[phases.build]
cmds = ["npm run build", "npm run db:push", "node fix-build.js"]

[start]
cmd = "npm start"