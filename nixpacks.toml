# Production configuration for Dokploy VPS deployment
providers = ["node"]

[variables]
NODE_ENV = "production"
PORT = "5000"

[phases.install]
cmds = ["npm ci --production=false"]

[phases.build]
cmds = [
  "npm run build",
  "node fix-build.js"
]

[start]
cmd = "node dist/index.js"