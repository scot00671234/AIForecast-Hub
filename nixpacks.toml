# ULTRA SIMPLE - Direct Node.js deployment
providers = ["node"]

[variables]
PORT = "5000"

[phases.install]
cmds = ["npm ci"]

[phases.build]  
cmds = ["chmod +x ultimate-deployment-fix.sh", "./ultimate-deployment-fix.sh"]

[start]
cmd = "node dist/index.js"