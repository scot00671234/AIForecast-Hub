# Force Node.js provider only
providers = ["node"]

[variables]
NODE_ENV = "production"
PORT = "3000"

[phases.install]
cmds = ["npm ci --production=false"]

[phases.build]
cmds = [
  "npm run build",
  "node fix-build.js"
]

[start]
cmd = "node dist/index.js"