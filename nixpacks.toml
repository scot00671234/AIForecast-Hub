# Simplified Node.js deployment configuration
providers = ["node"]

[variables]
NODE_ENV = "production"
PORT = "3000"

[phases.install]
cmds = ["npm ci --production=false"]

[phases.build]  
cmds = ["npm run build"]

[start]
cmd = "npm start"

[staticAssets]
enabled = "false"